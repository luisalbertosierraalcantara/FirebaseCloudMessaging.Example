# FirebaseCloudMessaging.Example
A simple .NET Core app server example for sending Push messages via Firebase Cloud Messaging (FCM).

-- install nuget package: 
       
-- NuGet\Install-Package FirebaseAdmin
